import {describe,it,expect} from 'vitest';import {applyEvent,fold,initialState} from './reducer';import {makeEvent} from './intents';import {circulation} from './selectors';import {Account,DEFAULT_CONFIG} from './types';
const bank:Account={id:'bank',kind:'bank',name:'Bank',color:'#000',unlimited:true,assets:[]};const p=(id:string):Account=>({id,kind:'player',name:id,color:'#fff',unlimited:false,assets:[]});
const started=()=>makeEvent('game.started',{config:DEFAULT_CONFIG,accounts:[bank,p('a'),p('b')]},'bank',0,'start');
describe('ledger',()=>{it('replays payments and bank circulation',()=>{let s=fold([started(),makeEvent('transfer',{from:'a',to:'b',amount:1700,reason:{kind:'other'}},'a',1,'x')]);expect(s.balances.a).toBe(-200);expect(s.balances.b).toBe(3200);expect(circulation(s)).toBe(3000)});it('deduplicates intent ids',()=>{const x=makeEvent('transfer',{from:'a',to:'b',amount:100,reason:{kind:'other'}},'a',1,'same');const s=fold([started(),x,x]);expect(s.balances.a).toBe(1400)});it('undoes only latest transfer',()=>{const es=[started(),makeEvent('transfer',{from:'a',to:'b',amount:200,reason:{kind:'other'}},'a',1,'1'),makeEvent('transfer',{from:'a',to:'b',amount:100,reason:{kind:'other'}},'a',2,'2'),makeEvent('transfer.reversed',{targetSeq:2},'bank',3,'3')];const s=fold(es);expect(s.balances.a).toBe(1300);expect(s.balances.b).toBe(1700)});it('elimination clears negative balance against bank',()=>{const es=[started(),makeEvent('transfer',{from:'a',to:'b',amount:1700,reason:{kind:'other'}},'a',1,'x'),makeEvent('player.eliminated',{accountId:'a',creditorId:'b'},'bank',2,'e')];const s=fold(es);expect(s.balances.a).toBe(0);expect(s.balances.b).toBe(3200);expect(s.eliminated.has('a')).toBe(true)})});
